name: Deploy

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and start container
        run: |
          docker compose up -d --build

      - name: Healthcheck
        run: |
          sleep 6
          docker ps | grep pet-shop-curso
          docker logs pet-shop-curso